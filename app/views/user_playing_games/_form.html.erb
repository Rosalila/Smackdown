<%= form_for(@user_playing_game) do |f| %>
  <% if @user_playing_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_playing_game.errors.count, "error") %> prohibited this user_playing_game from being saved:</h2>

      <ul>
      <% @user_playing_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.number_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :is_playing %><br>
    <%= f.check_box :is_playing %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
