<%= form_for(@community_point) do |f| %>
  <% if @community_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@community_point.errors.count, "error") %> prohibited this community_point from being saved:</h2>

      <ul>
      <% @community_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :community_id %><br>
    <%= f.number_field :community_id %>
  </div>
  <div class="field">
    <%= f.label :latitude %><br>
    <%= f.text_field :latitude %>
  </div>
  <div class="field">
    <%= f.label :longitude %><br>
    <%= f.text_field :longitude %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
