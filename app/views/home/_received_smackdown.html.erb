<% smackdown=locals[:smackdown] %>
<section class="content">
  <div class="row">
    <%= render 'smackdown_header', :locals => {:smackdown=> smackdown} %>
  </div>
  <div class="row">
    <%= render 'smackdown_info', :locals => {:smackdown=> smackdown} %>
    <%= render 'smackdown_rules', :locals => {:smackdown=> smackdown} %>
    <%= render 'share_smackdown', :locals => {:smackdown=> smackdown} %>
    <div class="col-lg-4 col-xs-6">
      <div class="box box-primary">
        <div class="box-header">
          <h3 class="box-title">Tu respuesta</h3>
        </div>
        <form role="form" accept-charset="UTF-8" action="/home/respond_smackdown" method="get" class="nifty_form">
          <div class="form-group">
            <label>Juez 2</label>
            <select class="form-control" name="judge_id">
              <% User.all.each do |judge| %>
                <% if judge.id != current_user.id %>
                  <option <%= "selected=\"selected\"" if smackdown.judge2_id == judge.id %> value="<%= judge.id %>"><%= judge.name %></option>
                <% end %>
              <% end %>
            </select>
          </div>
          <div class="form-group">
            <label>Respuesta</label>
            <select class="form-control" name="player2_accepted">
              <option <%= "selected=\"selected\"" if smackdown.player2_accepted %> value="true">Aceptar</option>
              <option <%= "selected=\"selected\"" if smackdown.player2_accepted == false %> value="false">Rechazar</option>
            </select>
          </div>
          <input name="smackdown_id" type="hidden" value="<%= smackdown.id %>" />
          <div class="box-footer">
            <button type="submit" class="btn btn-primary">Responder</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</section>
