<section class="content-header">
  <h1>
    Pendientes
    <small>responde los siguientes smackdowns</small>
  </h1>
</section>

<%= render 'toggle_all_smackdowns', :locals => {:smackdowns=> @smackdowns} if @smackdowns.count > 0 %>

<div class="box">
  <div class="box-header">
    <h3 class="box-title">Haz click para visitar a un usuario</h3>
  </div><!-- /.box-header -->
  <div class="box-body">
    <table id="example1" class="table table-bordered table-striped">
      <thead>
        <tr>
          <th>Juego</th>
          <th>Jugadores</th>
          <th>Informacion</th>
          <th>Responder</th>
          <th>Compartir</th>
        </tr>
      </thead>
      <tbody>
        <% @smackdowns.each do |smackdown| %>
          <tr>
            <%= render 'received_smackdown', :locals => {:smackdown=> smackdown} %>
          </tr>
        <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th>Juego</th>
          <th>Jugadores</th>
          <th>Informacion</th>
          <th>Responder</th>
          <th>Compartir</th>
        </tr>
      </tfoot>
    </table>
  </div><!-- /.box-body -->
</div><!-- /.box -->

<% if @smackdowns.count == 0 %>
  <p>No tienes smackdowns por responder. Pero puedes <a href="/home/users">Visitar</a> a otros usuarios y enviarles uno.</p>
  <% if !playingAtLeastAGame current_user %>
    <p>Todavía no has seleccionado tus juegos y reglas. ¡Ve a <a href="/home/profile">Mi Perfil</a> y selecionalos!</p>
  <% end %>
<% end %>
