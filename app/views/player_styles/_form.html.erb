<%= form_for(@player_style) do |f| %>
  <% if @player_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_style.errors.count, "error") %> prohibited this player_style from being saved:</h2>

      <ul>
      <% @player_style.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :long_description_link %><br>
    <%= f.text_field :long_description_link %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
