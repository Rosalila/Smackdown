<%= form_for(@rule_group) do |f| %>
  <% if @rule_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rule_group.errors.count, "error") %> prohibited this rule_group from being saved:</h2>

      <ul>
      <% @rule_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= collection_select(:rule_group, :game_id, Game.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
