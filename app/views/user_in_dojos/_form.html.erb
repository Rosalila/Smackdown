<%= form_for(@user_in_dojo) do |f| %>
  <% if @user_in_dojo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_in_dojo.errors.count, "error") %> prohibited this user_in_dojo from being saved:</h2>

      <ul>
      <% @user_in_dojo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :dojo_id %><br>
    <%= f.number_field :dojo_id %>
  </div>
  <div class="field">
    <%= f.label :is_admin %><br>
    <%= f.check_box :is_admin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
